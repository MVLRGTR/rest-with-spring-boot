spring:
  application:
    name: apirest

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.136:3306/apijava?useTimezone=true&serverTimezone=UTC
    username: apijava_user
    password: SenhaForteAqui

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        # Ajustado para MySQL, já que seu driver é MySQL
        #dialect: org.hibernate.dialect.MariaDBDialect   
logging:
  level:
    root: INFO
    com.digitalmindkr.apirest: DEBUG